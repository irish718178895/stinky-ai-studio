# Update to v0.5

Install FFmpeg first: `sudo apt install ffmpeg`. Copy the updated source files over v0.4 without replacing `data/`, run `npm install`, validate with `node --check server.js` and `node --check public/app.js`, then start with `npm start`.
