{
  "name": "stinky-ai-studio",
  "version": "0.6.0",
  "private": true,
  "description": "Local ComfyUI image studio with a cinematic FFmpeg renderer, transitions, progress, and GPU encoding.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
